title: Default
'@extends':
    type: default
    context: blueprints://pages
form:
  fields:
    tabs:
      type: tabs
      active: 1
      fields:
        broken_link_audit:
          type: tab
          ordering@: 3
          title: PLUGIN_BROKEN_LINK_AUDIT.ADMIN.TITLE
          fields:
            header.title2:
              type: broken-tab

